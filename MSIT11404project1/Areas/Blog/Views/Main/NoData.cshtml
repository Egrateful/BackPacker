@{
    Layout = "~/Views/Shared/_LayoutPro.cshtml";
}

@section style{
    <meta http-equiv="refresh" content="5;@Url.Action("BlogHome")" />
    }

<div class="text-center" style="width:auto;margin:50px auto">
    <h1 id="word">沒有相關搜尋結果，5秒後自動跳轉回首頁</h1>
    @Html.ActionLink("[或按此連結回去首頁]", "BlogHome")
</div>


@section script{
    <script>
        timego = setInterval(thetime, 1000);
         x = 5;
        function thetime() {
            word = document.getElementById("word")
            if (x >0) {
                x -= 1;
            }
            else {
                x=5
            }
            switch (x) {
                case 5:
                    word.innerHTML="沒有相關搜尋結果，5秒後自動跳轉回首頁"
                    break;
                case 4:
                    word.innerHTML = "沒有相關搜尋結果，4秒後自動跳轉回首頁"
                    break;
                case 3:
                    word.innerHTML = "沒有相關搜尋結果，3秒後自動跳轉回首頁"
                    break;
                case 2:
                    word.innerHTML = "沒有相關搜尋結果，2秒後自動跳轉回首頁"
                    break;
                case 1:
                    word.innerHTML = "沒有相關搜尋結果，1秒後自動跳轉回首頁"
                    break;
                case 0:
                    word.innerHTML = "沒有相關搜尋結果，0秒後自動跳轉回首頁"
                    break;
            }
        }

    </script>
    
    
    
    }